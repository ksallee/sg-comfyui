import{a as l,f as m}from"../chunks/B0iT6qcp.js";import"../chunks/Cbgw8pzA.js";import{n as t,l as A,J as I,v as c,$ as V,G as h,w as a,y as C,g as n,I as G,x as $,K as M}from"../chunks/B4r53-SI.js";import{s as p}from"../chunks/BdbJsSLX.js";import{e as O}from"../chunks/D3VIR5-p.js";import{h as U}from"../chunks/Bcg428hZ.js";import{S as j}from"../chunks/CPu_0Lxf.js";import{p as H}from"../chunks/CFv_lA_4.js";var N=m('<meta name="description" content="The nine typed fields on Version, where each value comes from, and what a site without them records instead."/>'),W=m("<tr><td> </td><td><code> </code></td><td> </td></tr>"),D=m(`<h1>Provenance fields</h1> <p class="lede">Nine typed fields on Version, created under Settings, then SG, SG Site Setup. A publish does not
	need them.</p> <table><thead><tr><th scope="col">field</th><th scope="col">programmatic name</th><th scope="col">from</th></tr></thead><tbody></tbody></table> <p><code>sg_ai_seed</code> is text, not a number: seeds reach 2**64-1, which no numeric field on the
	site has (probe 019).</p> <h2>Without the fields</h2> <ul><li>The facts go in the Version's description: the note, a blank line, then one line per fact, lineage included.</li> <li>Where some of the nine exist, those take their values. The description records the rest.</li> <li>The nodes say nothing about creating fields.</li></ul> <!> <h2>Attachments</h2> <ul><li>The record is attached as <code>.provenance.json</code>.</li> <li>The workflow is attached when the submitting client sent one.</li> <li>Provenance is scoped per branch. The node walks back through its own inputs.</li></ul> <h2>Creating the fields from the command line</h2> <pre><code>PYTHONPATH=src &lt;comfy-python&gt; -m comfyui_sg.fields</code></pre> <ul><li>It reads the schema first and creates only what is missing. Pressing twice is safe.</li> <li>It imports no torch.</li> <li>It reads a script key from <code>.env.local</code>, never from Settings.</li> <li>That key needs permission to create fields on Version. Most artist accounts do not have it.</li> <li>Field names are permanent. Deleting a field frees the field, never its name. Trashed fields cannot be listed, so a name spent here is spent site-wide forever (probe 019).</li></ul> <h2>Mapping to existing fields</h2> <p>Point the profile's <code>provenance.map</code> at them. Mapping onto a studio's own fields works
	and is not a release feature.</p> <h2>The submitting client</h2> <p>A Version that reads <code>ComfyUI (unknown client)</code> means the client that POSTed <code>/prompt</code> did not name itself. The name is the client's own claim, in <code>extra_data.comfy_usage_source</code>. Set it in your own submitter:</p> <pre><code>POST /prompt  &#123;"prompt": &#123;...&#125;, "extra_data": &#123;"comfy_usage_source": "my-farm-submitter"&#125;&#125;</code></pre> <p>A <code>Comfy-Usage-Source</code> header works too. The server copies it into <code>extra_data</code> only when the body left the key out.</p>`,1);function L(g){var f=D();U("9t7ju4",o=>{var e=N();I(()=>{V.title="Provenance fields. SG ComfyUI"}),l(o,e)});var r=t(A(f),4),u=t(c(r));O(u,5,()=>H,([o,e,i])=>e,(o,e)=>{var i=$(()=>M(n(e),3));let y=()=>n(i)[0],b=()=>n(i)[1],w=()=>n(i)[2];var s=W(),v=c(s),T=h(v,!0),d=t(v),x=c(d),S=h(x,!0);a(d);var k=t(d),P=h(k,!0);a(s),C(()=>{p(T,y()),p(S,b()),p(P,w())}),l(o,s)}),a(u),a(r);var _=t(r,8);j(_,{name:"05_publish_provenance_into_description",alt:"A Version's description holding the provenance facts, one per line, on a site with none of the nine fields",caption:"The note, a blank line, then one line per fact."}),G(22),l(g,f)}export{L as component};
