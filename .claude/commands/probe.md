---
description: Write and run a probe answering one question about the Flow PT REST API
---

Question: $ARGUMENTS

1. Pick the next free number in `probes/`.
2. Write `probes/NNN_slug.py` using `_lib` — one question, read-only unless the question needs `--write`.
3. Run it.
4. Fill the verdict in `probes/findings/NNN_slug.md` with what actually happened, including where the docs were wrong.
5. Report the verdict in one or two lines. Do not restate the file.
